# Argo gira quasi interamente in sola stdlib (urllib, sqlite3, concurrent.futures).
# Unica dipendenza esterna: pypdf, usata SOLO da argo/scadenza.py per leggere la
# scadenza dei bandi dai PDF allegati (di norma non e' nell'HTML della pagina).
# L'import e' protetto: se pypdf manca, la scadenza resta "ignota" e il resto del
# crawler funziona comunque.
pypdf>=4.0

# Revisione AI dei findings (argo/ai_review.py): legge il documento del bando e
# ricava scadenza affidabile + titolo pulito + conferma-bando via GPT-4o-mini.
# Import protetto: senza la libreria (o senza OPENAI_API_KEY) il passo si salta.
openai>=1.0
